package pages.locators;

import org.openqa.selenium.WebElement;
import org.openqa.selenium.support.FindBy;

public class HelpAndContactLocators {
    @FindBy(id = "account")
    public WebElement accountButton;
}
